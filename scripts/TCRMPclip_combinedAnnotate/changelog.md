# TCRMPclip_combinedAnnotate changelog

- 2026-06-30 21:05 AST — Step 4/4.test: flatten year folders + widen export manifest + host-open buttons + reliable one-click launch (subagent-driven plan, 8 code tasks + data-dict). FLATTEN: collapsed `session['segmentations_by_year']` → flat `session['segmentations']`; dropped `year` from `_seg_path`/`_manifest_path`/`load`/`save_segmentations`/`save_manifest`/`render_segmentation_overlays`/`_publish_preview`/`_find_segmentation`; on-disk now flat `segmentations/{segmentations.json,processed_manifest.json,overlays_{auto,reviewed}}`; `configure()`/`resume()` read flat `routed_input/{ids/sam_click_prompts.json,raw}` (orchestrator `_flatten_routed_input` merges placePoints' per-year output post-route); `status()` reports single `stats` (year_stats dropped). MANIFEST: `EXPORT_MANIFEST_FIELDS` += `batch_name` (`batch_<NNN>_<YYYYMMDD-HHMMSS>`, seq in `.export_batch_seq`) + `image_path`; dynamic per-species accepted-mask-count columns (bare code headers); `_read_export_manifest` preserves all columns, `write_export_manifest` unions species + backfills 0 across cumulative batches (data-dict schema 1.2.0). BUTTONS: `#export-overlay` gains host-only Open export folder / Open manifest (`/api/open_export_folder`, `/api/open_export_manifest` → `xdg-open`, path guarded to `export_dir`). LAUNCH: `orchestrated` gate honors `?orchestrated=1` OR env; `#orch-loading` → black status-bar loading screen; boot never shows `#startup` when orchestrated (Retry on configure failure, `_orchBooting` in-flight guard); orchestrator.js closes stale annotator window + opens `?orchestrated=1`. Tests: full suite 41/41 (new: test_open_routes, test_flatten_routed_input, test_flat_segmentations; updated: test_export_{manifest,previews}, test_batch_flow, test_segment_app, test_route_clears_stale). Orchestrator restarted on 5050 (self-test green). Live SAM3 browser click-test of the launch flow DEFERRED to Lauren (GPU+browser).

- 2026-06-30 AST — Task B2: added `rejected_ids: rejectedMaskIds()` to the `draw_edit` fetch body in `src/templates/index.html` (~line 3587), matching how `/add`, `/refine`, `/rebox` already send it. This ensures a fast delete→draw sequence is not clipped against a mask whose reject PUT has not landed yet. Test: `test_draw_edit_sends_rejected_ids` in `tests/test_segment_app.py` (first FAIL, then PASS). No `?v=` cache-bust found in the template or CA directory — not applicable. All 28 tests PASS.
- 2026-06-30 AST — Added `_clip_to_neighbors(new_binary, blocking)` helper in `src/app.py` (below `_blocking_masks`); repointed `draw_edit` to clip against live neighbors instead of calling `_steal_region_from_others`, so an edit is trimmed to free space (existing masks win). Deleted `_steal_region_from_others` entirely (was dead after repoint). `overlap_delta` is now always `{'updated': [], 'removed': []}` — neighbors are no longer modified by a draw edit. Test: `tests/test_draw_clip.py` (4/4 PASS), registered in `scripts/run_all_tests.sh`. Regression: `tests/test_segment_app.py` 27/27 PASS.
- 2026-06-30 08:50 AST — Added `_publish_preview(export_dir, year, stem)` helper in `src/app.py` (above `write_export_manifest`); copies reviewed overlay JPEG into flat `previews/` folder in export root, returns export-root-relative path or `""` if source missing. Test: `tests/test_export_previews.py` (3/3 PASS), registered in `scripts/run_all_tests.sh`.
- 2026-06-30 AST — Added `preview_file` column to `EXPORT_MANIFEST_FIELDS`; `do_export` manifest-build loop now calls `_publish_preview` per frame (skipped for `scrapped`/`image_missing` outcomes) and stores the relpath in each manifest row. `schema_version` not found in CA/src — no bump performed (no such field exists). Test: `tests/test_export_manifest.py` (9/9 PASS), `tests/test_export_previews.py` (3/3 PASS).
- 2026-06-30 09:16 AST — Task C3: added module-level helpers `_clear_routed_input` and `_routed_prompts_stale` to `scripts/pipeline_orchestrator/app.py` (column 0, lines 105/124); replaced the flat reuse guard in `_run_step4test` with a staleness-gated version that clears stale routed_input/ year-dirs and falls through to re-route when Step 3 selection is newer than cached prompts. Fast-reuse preserved when cache is fresh. Test: `scripts/pipeline_orchestrator/tests/test_route_clears_stale.py` (9/9 PASS, first FAIL then PASS). AST parse OK. Regressions: `test_step4test_clipdir.py` 4/4 PASS, `test_route_species_filter.py` 2/2 PASS.
- 2026-07-03 (LO): /add,/refine,/rebox report blocked_by mask id+species on full-overlap; frontend shows "Blocked by #N".
- 2026-07-03 (LO): regression lock — add after delete keeps the freed region (rejected_ids excluded from clip); live neighbors still protected.
- 2026-07-03 (LO): clean_mask closing kernel scales with image size (~15px@1920, ~30px@4K, cap 51) so large-colony fragments bridge instead of being dropped.
